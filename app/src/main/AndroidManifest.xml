<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="template.entelect.co.za.template">

    <application
        android:name=".MainApplication"
        android:allowBackup="true"
        android:hardwareAccelerated="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <meta-data
            android:name="MAPPING_FACTORY"
            android:value="template.entelect.co.za.template.domain.ColumnMappingFactory" />
        <meta-data
            android:name="CPORM_CONFIG"
            android:value="template.entelect.co.za.template.domain.DatabaseConfiguration" />
        <meta-data
            android:name="AUTHORITY"
            android:value="za.co.omnia.gis.provider" />

        <activity android:name=".presentation.main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>
